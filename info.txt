remove containers
	sudo docker rm -v -f $(sudo docker ps -a -q -f status=exited)
	sudo docker rm -v -f $(sudo docker ps -a -q)

remove all images
	sudo docker rmi -f $(sudo docker images -q)

todays ip

		52.209.103.149

-jenkins
-jira
-nexus
-zabbix
 - need to get zabbix server running
tomcat
postfix
urbanCode-deploy

docker-compose
	look at updating it to use version 2
